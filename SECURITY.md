# Security

## 支持范围

当前为 0.1.x 预览版，安全修复以最新版本为目标，没有响应时限或服务等级承诺。发布者应及时处理依赖告警，并维护 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。

## 威胁边界

- 应用只绑定 `127.0.0.1`，不是面向互联网或局域网的服务，不应通过反向代理或端口转发暴露。
- Host、Origin 校验及修改请求令牌用于降低跨站请求风险；它们不隔离同一账户下的恶意本机程序，也不提供多用户访问控制。
- 扫描范围由用户配置。为支持共享技能，会跟随目录联接和符号链接到实际位置；这些链接可能指向目录外，因此来源目录必须由用户信任。来源选择不是文件系统沙箱。
- 仅读取技能入口文档，不执行技能脚本。文件读取有单文件、总量、深度和一致性检查，异常扫描不会伪造卸载事件。
- Markdown 使用白名单清洗，不运行脚本、表单或嵌入页面，不自动加载远程图片。外链仅在用户点击时打开；未开放相对路径读取其他本机文件。
- 运行期间不调用云端模型或发送遥测。安装依赖需要访问配置的 npm 源；浏览器打开文档外链属于单独的用户操作。
- `data/`、本地配置、备份和导出 JSON 可能包含个人路径、技能描述及笔记。忽略规则不等于加密，也不能撤销已公开的内容。
- 发布检查只做允许清单、已跟踪文件路径与高置信度敏感模式检查，不替代人工审查、完整秘密扫描或 Git 历史审计。

## 私密报告

公开仓库的维护者必须在发布前启用 GitHub Private vulnerability reporting。报告者可在仓库的 **Security → Report a vulnerability** 提交私密报告；不要在公开 Issue 或 PR 中披露未修复漏洞、真实凭据或用户资料。

如果仓库尚未提供私密报告入口，请先通过已公开的维护者联系渠道询问安全联系方式，不公开技术细节。此源码版本不预设个人邮箱，也不声称远程报告渠道已经启用。

请提供受影响版本、攻击前提、使用虚构数据的最小复现、预期与实际行为，以及影响范围。不要上传真实技能库或完整用户目录；发现凭据暴露时先由凭据所有者轮换凭据。
