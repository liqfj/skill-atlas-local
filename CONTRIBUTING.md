# Contributing

欢迎提交问题、文档和小范围改进。提交贡献前请确认你有权按项目的 MIT 许可证公开这些内容；不要引入所在单位的内部技能、业务数据或未获授权的文档。

## 本地开发

使用 Node.js 22 或 24：

```powershell
npm ci --ignore-scripts --no-audit --no-fund
npm test
npm run check
npm run licenses:check
npm run release:check
```

`npm start` 启动本地服务，前端无需构建。服务端改动需要重启。不要在服务运行时删除实例锁或运行离线扫描。

浏览器验收优先运行 `node tests/browser-fixture.mjs`，使用它输出的独立地址。样例包含独立技能、主从技能包、集合和候选分组，不依赖个人安装目录；`Ctrl+C` 会清理临时数据。界面改动检查桌面及至少 320px 宽度下的布局，并覆盖失败和空状态。

## 改动约定

- 原始技能目录只读，不运行其中的脚本，不将原文提交到仓库。
- 包归属独立于用途分类；重分类不能拆散技能包，保留用户笔记和人工决定。
- 所有来自文件、YAML、Markdown 和本地配置的内容均视为不可信数据。
- 沿用 Node.js ESM、原生测试运行器及现有工具，避免为小功能引入大型框架。
- 使用临时目录测试持久化，不改真实清单；新增行为先添加能区分正确与错误实现的用例。
- 依赖必须锁定版本，核对许可与安全维护状态。依赖调整后运行 `npm run licenses:write` 并人工检查声明差异；不要忽略许可校验失败。
- 新增可公开文件必须明确加入 [package.json](package.json) 的 `files` 清单。私有配置、截图原始数据、缓存和导出物不能加入该清单。
- 涉及数据格式变化时添加迁移与回退测试，先备份后迁移，不以清空数据代替兼容升级。

## 报告问题

使用问题模板，提供版本、操作系统、Node.js 版本、最小复现及预期行为。只提交虚构或充分脱敏的样例；不要附带整个 `data/`、个人路径、真实技能原文、会话日志或凭据。安全问题按 [SECURITY.md](SECURITY.md) 私下报告。

PR 说明应包含行为变化、实际运行的验证和未验证范围。跨平台 CI 结果必须如实说明，不能把 Windows 本地通过当作其他系统已验证。
