# Release Checklist

本项目首发以源码预览版为目标。下列命令仅验证和生成本地产物，不创建远程仓库、不提交、不打标签、不推送、不上传，也不运行 `npm publish`。

## 准备

1. 确认项目代码、中文参考简介、截图和其他附带材料均有权公开。项目采用 [MIT](LICENSE)；第三方技能本身不因此改用本项目许可。
2. 核对 [package.json](package.json) 的版本、Node.js 支持范围和显式 `files` 清单。该清单必须包含锁文件、许可证、运行源码、验证脚本和公开文档。
3. 检查默认 [config.json](config.json) 只包含可公开的 `~/` 路径。个人来源、设置与扩展词条放在忽略的 `data/` 下。
4. 截图仅使用 `node tests/browser-fixture.mjs` 的虚构样例。不要截图真实来源目录、私人描述或笔记。
5. 在公开仓库设置中启用私密漏洞报告、Secret scanning / Push protection（可用时）以及所需分支保护。这些仓库设置不能由本地文档代替。

## 验证

```powershell
npm ci --ignore-scripts --no-audit --no-fund
npm test
npm run check
npm run licenses:check
npm run release:check
npm run release
```

依赖变化后先运行 `npm run licenses:write`，人工审阅 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)，再执行上述验证。当前 `launder 1.7.1` 上游仅提供 MIT 元数据声明，没有独立许可证文件；声明明确列出固定提交与完整性值。这不构成法律审核，发布包含其实现的捆绑构建前应另行核实许可义务。源码归档不含任何依赖实现。

CI 定义 Windows、macOS、Linux 与 Node.js 22 / 24 矩阵，动作固定到提交，权限仅为只读。正式宣称某平台受支持前，必须取得该版本的实际 CI 结果；配置文件存在不等于已经运行。依赖安全扫描需要另外检查当时的告警，许可证通过不表示没有漏洞。

## 本地产物

`npm run release` 在 `artifacts/` 生成带内容摘要的 `.tgz` 源码包和 `.manifest.json` 文件。构建从允许清单读取文件快照，拒绝链接及私有路径，再核对归档中每个成员的路径、大小和 SHA-256。相同文件快照生成相同归档摘要。

发布前检查 manifest 的完整文件列表及归档摘要，并在干净临时目录解压后执行安装、测试及启动验证。测试启动使用空来源或临时来源，不读取发布者的个人技能目录。

禁止上传整个工作区，禁止上传 `data/`、`node_modules/`、配置备份、日志、凭据、开发会话或真实技能原文。只共享审查通过的源码归档及其 manifest。打包脚本不会自动上传；向第三方发送前应重新检查实际待发送内容。

## Git 与公开操作

- 发布检查会拒绝已跟踪但未在公开允许清单中的文件；没有 Git 仓库时明确报告未初始化，不自动创建仓库。
- 当前源码检查不扫描 Git 历史。导入已有历史时，用可信秘密扫描工具检查全部拟公开历史，并人工复核。删除最新文件或增加忽略规则不能清除历史泄漏。
- 第一次提交前检查完整暂存差异，尤其是本地配置和生成数据；不要直接全目录添加。用户数据禁止强制加入。
- 由仓库所有者明确授权后才执行创建远程仓库、提交、打标签或发布。发布说明引用 [CHANGELOG.md](CHANGELOG.md)，标明预览状态、已验证平台和已知限制。

## 数据升级

停止应用，私下备份 `data/` 后再更新源码和锁定依赖。保持清单、包归属、人工分类、中文修订和笔记不变。当前只接受版本 1 的清单，未知版本或损坏内容拒绝加载，不清空重建。将来引入新格式前必须提供显式迁移、失败保护和对应测试。
